<Page x:Class="InterfaceAAD.Views.ClientEditView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      xmlns:local="clr-namespace:InterfaceAAD.Views"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="850"
      Title="TestePagina">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="4*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>


        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="47*" />
            <ColumnDefinition Width="213*" />
        </Grid.ColumnDefinitions>


        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" FontSize="24" Margin="0,0,0,30" Grid.RowSpan="2">Utilizador</TextBlock>

        <Label Grid.Row="1" Grid.Column="0">Nome *</Label>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="5,0,5,10" Text="{Binding SelectedClient.ClienteNome}" />
        <Label Grid.Row="1" Grid.Column="0" Margin="0,30,0,34" Grid.RowSpan="3">NIF *</Label>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="5,30,5,5" Text="{Binding SelectedClient.ClienteNIF}" Grid.RowSpan="2" />
        <Label Grid.Row="3" Grid.Column="0" Margin="0,0,0,30" Grid.RowSpan="2">Data Nascimento *</Label>
        <DatePicker Grid.Row="3" Grid.Column="1" Margin="5,0,5,10" SelectedDate="{Binding SelectedClient.ClienteDataNasc, StringFormat='dd/MM/yyyy'}" />
        <Label Grid.Row="4" Grid.Column="0">Morada *</Label>
        <TextBox Grid.Row="4" Grid.Column="1" Margin="5,0,5,10" Text="{Binding SelectedClient.ClienteMorada}" />
        <Label Grid.Row="4" Grid.Column="0" Margin="0,30,0,30" Grid.RowSpan="3">Código Postal *</Label>
        <TextBox Grid.Row="4" Grid.Column="1" Margin="5,30,5,5" Text="{Binding SelectedClient.CPCP}" Grid.RowSpan="2" />

        <Label Grid.Row="5" Grid.Column="0" Margin="0,30,0,30" Grid.RowSpan="3">Adicionar Contacto*</Label>
        <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal" Margin="5,26,0,4" Grid.RowSpan="2">
            <TextBlock Text="Selecionar Tipo de Contato:" VerticalAlignment="Center" Margin="0,0,10,0" />
            <ComboBox ItemsSource="{Binding TipoContacto}" 
          DisplayMemberPath="DescTpContacto" 
          SelectedItem="{Binding SelectedContactType, Mode=TwoWay}" 
          Width="60"
          Height="21" />
            <TextBlock Text="Novo Contato:" VerticalAlignment="Center" Margin="10,0,10,0" />
            <TextBox Text="{Binding NovoContato, Mode=TwoWay}" Width="200" Height="22" RenderTransformOrigin="0.5,0.5" >
                <TextBox.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform/>
                        <RotateTransform Angle="0.19"/>
                        <TranslateTransform/>
                    </TransformGroup>
                </TextBox.RenderTransform>
            </TextBox>
            <Button Content="&#xE109;" FontFamily="Segoe UI Symbol" FontSize="16" Command="{Binding AdicionarContatoCommand}" Click="Button_Click" Width="26" Height="25"/>
            <!-- Ajuste o valor de Width conforme necessário -->
        </StackPanel>

        <DataGrid Grid.Row="6" Name="myDataGrid" AutoGenerateColumns="False" ItemsSource="{Binding SelectedClient.ClientContacts}" CanUserAddRows="False" Margin="150,30,9,0" VerticalAlignment="Top" Grid.ColumnSpan="2" Grid.RowSpan="2">
            <DataGrid.Columns>
                <!-- Coluna do Tipo de Contato (não editável) -->
                <DataGridTemplateColumn Header="Tipo de Contato">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding TipoContacto.DescTpContacto}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <!-- Coluna do Contato (editável) -->
                <DataGridTextColumn Header="Contato" Binding="{Binding ContactoCliente, Mode=TwoWay}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>


        <!-- Adicione outros controles conforme necessário para outros detalhes do cliente -->


        <StackPanel Orientation="Horizontal" Grid.Row="9" Grid.Column="1">
            <Button Width="150" Content="Voltar" Click="BackButton_Click" Margin="5" />
            <Button Width="150" Content="Cancelar" Command="{Binding CancelCommand}" Margin="5" />
            <Button Width="150" Content="Guardar" Command="{Binding SaveCommand}" Margin="5" />
        </StackPanel>

    </Grid>
</Page>